# ---
# - name: Setup Python to enable Ansible configuration.
#   hosts: drone
#   become: True
#   become_user: root
#   gather_facts: False
#   vars_files:
#     - ["vars/main.yml"]
#   roles:
#     - {role: angstwad.docker_ubuntu, pip_version_docker_py: 1.9.0}
#   pre_tasks:
#     - name: Setup python 2 to make Ansible work with Ubuntu.
#       raw: |
#         test -e /usr/bin/python ||
#         (apt -y update && apt install -y python-minimal)
#     - setup:
